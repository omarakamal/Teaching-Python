#string method
# .strip() strips the spaces before and aftet the string
# len() gives you the length of the characters in the string. its a function
# .split() creates a list out of the string you give it
#.upper makes the letters uppercase
#lower (makes the letters lowercase)

text = input("input something: ")
print(text.split(' '))
